{
  "name": "webapp",
  "version": "0.1.0",
  "description": "the webapp for werwolf-online (no framework)",
  "main": "dist/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel *.html",
    "dev-watch": "parcel watch admin_ui/**/*.html user_ui/**/*.html --public-url /static/"
  },
  "author": "lokmeinmatz",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "parcel": "^1.12.4",
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.7.5"
  }
}
